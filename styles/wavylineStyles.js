import { StyleSheet } from 'react-native';

export const wavylineStyles = StyleSheet.create({
    container: {
        marginVertical: 10, // Adjust the vertical margin as needed
        alignItems: 'center',
    },
});
